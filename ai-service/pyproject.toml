[project]
name = "visiocheck-ai"
version = "0.1.0"
description = "Microservice d'inférence VisioCheck (détection, suivi, description)"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["visiocheck_ai*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
