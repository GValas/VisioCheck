# gRPC + protobuf
grpcio>=1.62
grpcio-tools>=1.62
protobuf>=4.25

# Vision / détection
ultralytics>=8.3.0      # YOLO11/YOLO26 + ByteTrack intégré
pillow>=10.0
numpy>=1.26

# torch est fourni par l'image de base CUDA (voir Dockerfile) ; en local CPU :
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# VLM (description de scène) — requiert un GPU NVIDIA.
# Désactivable via VC_VLM_ENABLED=false (mode dégradé sans cette dépendance).
vllm>=0.6.3 ; platform_system == "Linux"

# WebRTC (transport média alternatif). Désactivable via VC_WEBRTC_ENABLED=false ;
# si aiortc est absent, seul le transport WebSocket reste disponible.
aiortc>=1.9.0

# Dev / tests
pytest>=8.0
